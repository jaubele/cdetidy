% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_primary_key.R
\name{validate_primary_key}
\alias{validate_primary_key}
\title{Validate primary key uniqueness in a dataset}
\usage{
validate_primary_key(
  data,
  key_cols,
  sample_n = 10000,
  full_run = FALSE,
  seed = 1234
)
}
\arguments{
\item{data}{A data frame to check.}

\item{key_cols}{A character vector of column names that should act as the primary key.}

\item{sample_n}{Integer. Number of rows to sample for validation (if \code{full_run = FALSE}). Default is 10,000.}

\item{full_run}{Logical. If \code{TRUE}, checks the entire dataset. Default is \code{FALSE}.}

\item{seed}{An integer used to seed the random sample (only applies if \code{full_run = FALSE}). Default is 1234.}
}
\value{
Invisibly returns \code{NULL}. Prints a success or failure message indicating whether the key uniquely identifies rows.
}
\description{
Checks whether a set of columns uniquely identify rows in a data frame.
Can be run on a full dataset or a sampled subset to improve performance on large data.
}
\details{
\itemize{
\item Columns listed in \code{key_cols} must all exist in \code{data}.
\item If \code{full_run = FALSE} and the dataset has more than \code{sample_n} rows, a sample is used.
\item Uses \code{anyDuplicated()} for fast duplicate detection.
\item Messages use colored console output (green = valid, red = invalid).
}
}
\examples{
validate_primary_key(mydata, key_cols = c("student_id", "year"))

}
