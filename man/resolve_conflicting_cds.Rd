% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_conflicting_cds.R
\name{resolve_conflicting_cds}
\alias{resolve_conflicting_cds}
\title{Identify and resolve conflicting CDS codes in a dataset}
\usage{
resolve_conflicting_cds(df, cds_col, org_level_col)
}
\arguments{
\item{df}{A data frame containing CDS codes and organization level indicators.}

\item{cds_col}{A string specifying the column name containing the CDS code.}

\item{org_level_col}{A string specifying the column name containing the organization level (e.g., \code{"C"}, \code{"D"}, \code{"S"}).}
}
\value{
A modified data frame with resolved CDS values. A \code{"conflicting_cds"} attribute is attached
to the returned data frame, containing the list of detected conflicts.
}
\description{
Wraps \code{get_conflicting_cds()} and \code{replace_conflicting_cds()} to identify duplicate CDS codes
that span multiple organization levels and resolve them by appending a suffix (e.g., \code{"9999"})
to the school-level (\code{"S"}) CDS codes. The resulting dataset preserves uniqueness while maintaining traceability.
}
