% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_cds_codes.R
\name{expand_cds_codes}
\alias{expand_cds_codes}
\title{Expand a full CDS code into county, district, and school components}
\usage{
expand_cds_codes(
  df,
  cds_col = "cds",
  county_col = "county_code",
  district_col = "district_code",
  school_col = "school_code"
)
}
\arguments{
\item{df}{A data frame containing a column with full CDS codes.}

\item{cds_col}{The name of the CDS column. Default is \code{"cds"}.}

\item{county_col}{The name of the column to store the county code. Default is \code{"county_code"}.}

\item{district_col}{The name of the column to store the district code. Default is \code{"district_code"}.}

\item{school_col}{The name of the column to store the school code. Default is \code{"school_code"}.}
}
\value{
A data frame with new columns for county, district, and school codes.
}
\description{
Splits a 14-character CDS code into its component parts:
county (2 digits), district (5 digits), and school (7 digits).
Pads each component to ensure correct width using leading zeros.
}
\examples{
df <- data.frame(cds = "01023000400567")
expand_cds_codes(df)

}
